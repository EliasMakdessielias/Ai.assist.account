{
  "name": "bokpilot-email-worker",
  "private": true,
  "type": "module",
  "description": "Kö-processor för email-notiser: hämtar pending-poster ur notification_queue, skickar via SMTP (nodemailer), spårar leverans och retryar med exponentiell backoff.",
  "scripts": {
    "send": "node index.mjs",
    "test-smtp": "node index.mjs --test",
    "verify": "node index.mjs --verify"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "nodemailer": "^6.9.14"
  }
}
