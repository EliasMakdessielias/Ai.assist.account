# Supabase Edge Function-secrets (sätt med: supabase secrets set NAME=value).
# SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY / SUPABASE_ANON_KEY injiceras automatiskt.
# INGA värden checkas in – detta är endast en mall.

# ── Stripe (billing) ──────────────────────────────────────────────
# Aktiverar checkout + webhook. Utan dessa returnerar checkout {configured:false}
# och kunden får supportärende-fallback. Price-id:n läggs på subscription_plans
# (stripe_price_monthly/yearly) via Billing → Planer, inte här.
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_SUCCESS_URL=https://app.bokpilot.se/installningar/abonnemang?checkout=success
STRIPE_CANCEL_URL=https://app.bokpilot.se/installningar/abonnemang?checkout=cancel
# Valfria referenser (price-id:n bor primärt i subscription_plans):
STRIPE_PRICE_STARTER_MONTHLY=price_...
STRIPE_PRICE_STARTER_YEARLY=price_...
STRIPE_PRICE_PRO_MONTHLY=price_...
STRIPE_PRICE_PRO_YEARLY=price_...
STRIPE_PRICE_BYRA_MONTHLY=price_...
STRIPE_PRICE_BYRA_YEARLY=price_...

# ── Inbound email (webhook -> documents) ──────────────────────────
INBOUND_EMAIL_WEBHOOK_SECRET=

# ── Notiser ───────────────────────────────────────────────────────
NOTIF_UNSUB_SECRET=          # avregistreringslänk (notif-unsubscribe)
ERROR_REPORT_SECRET=         # HMAC för report-error (IMAP-importern)

# ── AI/OCR (primär: Gemini) ───────────────────────────────────────
GEMINI_API_KEY=

# ── OCR-provider-arkitektur (feature flags) ───────────────────────
# Styr vilken provider som används. Gemini är ALLTID primär i produktion.
# Dessa flaggor påverkar endast den modulära orkestreringen/testverktyget –
# det befintliga tolka-underlag-flödet körs oförändrat oavsett.
OCR_PROVIDER_PRIMARY=gemini
OCR_PROVIDER_SECONDARY=folio_ocr
ENABLE_OCR_FALLBACK=true        # vid Folio-fel: fall tillbaka till Gemini

# ── Folio-OCR (valfri, experimentell, SEPARAT tjänst) ─────────────
# Default AV. Aktiveras endast när tjänsten finns och ska testas.
# Utan ENABLE_FOLIO_OCR=true ELLER utan FOLIO_OCR_BASE_URL returnerar
# ocr-folio {available:false} och inget händer (safe uninstall).
ENABLE_FOLIO_OCR=false
FOLIO_OCR_BASE_URL=             # t.ex. https://folio.intern.example.se  (adapter-API: POST /ocr, GET /health)
FOLIO_OCR_TIMEOUT_MS=20000
FOLIO_OCR_API_SECRET=           # valfri; skickas som X-Api-Key till Folio-tjänsten
